import React from "react";

type ChipProps = {
  label: string;
};

const Chip = ({label}: ChipProps) => {
  return (
    <>
    {/* Chip from "@mui/material/Chip" */}
    </>
  );
};

export default Chip;