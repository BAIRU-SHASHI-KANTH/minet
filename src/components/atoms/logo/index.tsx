import React from "react";

type LogoProps = {
  src: string;
  alt: string;
};

const Logo = ({src,alt}: LogoProps) => {
  return (
   <>
   {/* img tag with LogoProps */}
   </>
  );
};
export default Logo;