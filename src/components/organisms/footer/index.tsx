import { Box, Grid } from '@mui/material';
import React from 'react'

interface FooterProps {
    links:object[]
}
const Footer = ({links}:FooterProps) => {
  return (
        <Grid container direction="row">
        {/* map links and button for need help */}
      </Grid>
  );
}

export default Footer